% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap.R
\name{phint_unoverlap}
\alias{phint_unoverlap}
\title{Resolve overlapping intervals sequentially or by priority}
\usage{
phint_unoverlap(
  phint,
  priority = NULL,
  priority_order = c("asc", "desc", "appearance"),
  within_priority = c("sequential", "keep"),
  na_propagate = FALSE
)
}
\arguments{
\item{phint}{\verb{[phinterval / Interval]}

A \verb{<phinterval>} or \verb{<Interval>} vector.}

\item{priority}{\verb{[vector / NULL]}

An optional grouping vector defining priority groups. Earlier groups (per
\code{priority_order}) are processed first and block later groups. Must be
recyclable with \code{phint}. If \code{NULL} (the default), all elements are resolved
considered to be within the same group.

\code{priority} may be any vector in the vctrs sense. See \code{\link[vctrs:vector-checks]{vctrs::obj_is_vector()}}
for details.}

\item{priority_order}{\verb{["asc" / "desc" / "appearance"]}

How to order priority groups for processing:
\itemize{
\item \code{"asc"} (default): Lower values are processed first (priority 1 before 2).
\item \code{"desc"}: Higher values are processed first (priority 9 before 2).
\item \code{"appearance"}: Groups are processed in order of first appearance in \code{priority}.
}}

\item{within_priority}{\verb{["sequential" / "keep"]}

How to handle overlaps within the same priority group:
\itemize{
\item \code{"sequential"} (default): Overlaps within a group are resolved by row
order, so earlier elements block later elements within the same group.
\item \code{"keep"}: Overlaps within a group are preserved; only overlaps with
higher-priority groups are removed.
}}

\item{na_propagate}{\verb{[FALSE / TRUE]}

Whether \code{NA} elements propagate to subsequent elements:
\itemize{
\item \code{FALSE} (default): \code{NA} elements are left as-is and do not affect subsequent
results.
\item \code{TRUE}: An \code{NA} element causes all subsequent elements (or lower-priority
group elements) to become \code{NA}.
}}
}
\value{
A \verb{<phinterval>} vector the same length as \code{phint}, where no two
elements overlap.
}
\description{
\code{phint_unoverlap()} removes overlaps across elements of a \verb{<phinterval>}
vector by trimming each element against all preceding elements. The result
is a vector where no two elements share any time.

Without \code{priority}, each element is trimmed by the union of all previous
elements:

\if{html}{\out{<div class="sourceCode">}}\preformatted{result[i] = phint_setdiff(phint[i], phint_squash(phint[1:(i - 1)]))
}\if{html}{\out{</div>}}

With \code{priority}, elements are grouped and processed in \code{priority_order}.
Each element is trimmed by all elements from earlier priority groups. Within
a priority group, \code{within_priority} controls whether overlapping elements
within each group are kept as-is or trimmed.
}
\examples{
monday <- interval(as.Date("2025-11-10"), as.Date("2025-11-11"))
tuesday <- interval(as.Date("2025-11-11"), as.Date("2025-11-12"))
wednesday <- interval(as.Date("2025-11-12"), as.Date("2025-11-13"))
mon_to_wed <- interval(as.Date("2025-11-10"), as.Date("2025-11-13"))
mon_to_tue <- interval(as.Date("2025-11-10"), as.Date("2025-11-12"))

# Sequential removal: each element is trimmed by all previous elements
phint_unoverlap(c(wednesday, mon_to_wed, mon_to_tue))

# Priority-based: lower priority values are processed first
phint_unoverlap(
  c(mon_to_wed, mon_to_tue, wednesday),
  priority = c(1, 2, 1)
)

# within_priority = "keep": overlaps within a group are preserved
phint_unoverlap(
  c(mon_to_wed, mon_to_tue, wednesday),
  priority = c(1, 1, 2),
  within_priority = "keep"
)

# priority_order = "desc": higher priority values are processed first
phint_unoverlap(
  c(mon_to_wed, mon_to_tue, wednesday),
  priority = c(1, 2, 1),
  priority_order = "desc"
)

# NA elements are treated as ignored by default
phint_unoverlap(c(mon_to_wed, NA, wednesday))

# NA elements propagate forward with na_propagate = TRUE
phint_unoverlap(c(mon_to_wed, NA, wednesday), na_propagate = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=phint_has_overlaps]{phint_has_overlaps()}} to test whether a \verb{<phinterval>} vector has
cross-element overlaps.
}
}
