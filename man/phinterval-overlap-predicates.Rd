% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlap.R
\name{phinterval-overlap-predicates}
\alias{phinterval-overlap-predicates}
\alias{phint_has_overlaps}
\alias{phint_any_overlaps}
\title{Test whether a phinterval vector has cross-element overlaps}
\usage{
phint_has_overlaps(
  phint,
  priority = NULL,
  priority_order = c("asc", "desc", "appearance"),
  within_priority = c("sequential", "keep"),
  na_propagate = FALSE
)

phint_any_overlaps(
  phint,
  priority = NULL,
  priority_order = c("asc", "desc", "appearance"),
  within_priority = c("sequential", "keep"),
  na_propagate = FALSE
)
}
\arguments{
\item{phint}{\verb{[phinterval / Interval]}

A \verb{<phinterval>} or \verb{<Interval>} vector.}

\item{priority}{\verb{[vector / NULL]}

An optional grouping vector defining priority groups. Earlier groups (per
\code{priority_order}) are processed first and block later groups. Must be
recyclable with \code{phint}. If \code{NULL} (the default), all elements are resolved
considered to be within the same group.

\code{priority} may be any vector in the vctrs sense. See \code{\link[vctrs:vector-checks]{vctrs::obj_is_vector()}}
for details.}

\item{priority_order}{\verb{["asc" / "desc" / "appearance"]}

How to order priority groups for processing:
\itemize{
\item \code{"asc"} (default): Lower values are processed first (priority 1 before 2).
\item \code{"desc"}: Higher values are processed first (priority 9 before 2).
\item \code{"appearance"}: Groups are processed in order of first appearance in \code{priority}.
}}

\item{within_priority}{\verb{["sequential" / "keep"]}

How to handle overlaps within the same priority group:
\itemize{
\item \code{"sequential"} (default): Overlaps within a group are resolved by row
order, so earlier elements block later elements within the same group.
\item \code{"keep"}: Overlaps within a group are preserved; only overlaps with
higher-priority groups are removed.
}}

\item{na_propagate}{\verb{[FALSE / TRUE]}

Whether \code{NA} elements propagate to subsequent elements:
\itemize{
\item \code{FALSE} (default): \code{NA} elements are treated as \code{\link[=hole]{hole()}}s and do not
affect subsequent results.
\item \code{TRUE}: An \code{NA} element causes all subsequent elements (or lower-priority
group elements) to become \code{NA}.
}}
}
\value{
\code{phint_has_overlaps()} returns a logical vector the same length as \code{phint}:
\itemize{
\item \code{TRUE}: the element would be modified by \code{\link[=phint_unoverlap]{phint_unoverlap()}} or blocks a
subsequent element.
\item \code{FALSE}: the element would not be affected by \code{\link[=phint_unoverlap]{phint_unoverlap()}}.
\item \code{NA}: the element is \code{NA}, or would become \code{NA} due to propagation when
\code{na_propagate = TRUE}.
}

\code{phint_any_overlaps()} returns a single \code{TRUE} or \code{FALSE}.
}
\description{
\code{phint_has_overlaps()} returns a logical vector indicating which elements
of \code{phint} would be modified by \code{\link[=phint_unoverlap]{phint_unoverlap()}} or are blockers of a
subsequent element. \code{phint_any_overlaps()} is a fast scalar equivalent to
\code{any(phint_has_overlaps(...), na.rm = TRUE)}.

Both functions accept the same arguments as \code{\link[=phint_unoverlap]{phint_unoverlap()}} and use the
same priority and within-priority resolution rules.

The following invariants hold:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# phint_unoverlap() ensures that phint_has_overlaps() is FALSE
phint <- phint_unoverlap(phint, ...)
!any(phint_has_overlaps(phint, ...), na.rm = TRUE)

# phint_unoverlap() does not alter non-overlapping elements
overlapping <- phint_has_overlaps(phint, ...)
all(phint[!overlapping] == phint_unoverlap(phint, ...)[!overlapping])

# phint_any_overlaps() is equivalent to any(phint_has_overlaps(...))
phint_any_overlaps(phint, ...) == any(phint_has_overlaps(phint, ...), na.rm = TRUE)
}\if{html}{\out{</div>}}
}
\examples{
monday <- interval(as.Date("2025-11-10"), as.Date("2025-11-11"))
tuesday <- interval(as.Date("2025-11-11"), as.Date("2025-11-12"))
wednesday <- interval(as.Date("2025-11-12"), as.Date("2025-11-13"))
mon_to_wed <- interval(as.Date("2025-11-10"), as.Date("2025-11-13"))
mon_to_tue <- interval(as.Date("2025-11-10"), as.Date("2025-11-12"))

# No overlaps: all FALSE
phint_has_overlaps(c(monday, tuesday, wednesday))
phint_any_overlaps(c(monday, tuesday, wednesday))

# Overlapping: blocker and blocked are both TRUE
phint_has_overlaps(c(mon_to_wed, mon_to_tue))
phint_any_overlaps(c(mon_to_wed, mon_to_tue))

# Non-overlapping elements are FALSE even when others overlap
phint_has_overlaps(c(mon_to_wed, mon_to_tue, wednesday))

# Priority-based: same rules as phint_unoverlap()
phint_has_overlaps(
  c(mon_to_wed, mon_to_tue, wednesday),
  priority = c(1, 2, 1)
)

# NA elements return NA
phint_has_overlaps(c(mon_to_wed, NA, wednesday))

# na_propagate = TRUE: NA propagates forward
phint_has_overlaps(c(monday, NA, wednesday), na_propagate = TRUE)
phint_any_overlaps(c(monday, NA, wednesday), na_propagate = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=phint_unoverlap]{phint_unoverlap()}} to resolve the overlaps detected by this function.
}
}
